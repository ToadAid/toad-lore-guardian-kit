# Build Reports

The full public v1 upload places these generated reports in this folder:

- `build-report.json` — corpus counts, platform file checks, volume hashes, and validation results
- `excluded-files.json` — source Markdown files excluded from the public corpus, with deterministic reasons

The reports make the corpus build inspectable without treating excluded drafts, templates, duplicate records, or internal builder material as public lore knowledge.
