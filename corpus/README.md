# Corpus Index

The full public v1 upload places `record-index.json` in this folder.

The index is a compact lookup surface for all 4,690 included records. It preserves record IDs, dates, document kinds, source references, hashes, evidence-layer metadata, status flags, and assigned platform volumes without duplicating the complete Markdown body of every scroll.
