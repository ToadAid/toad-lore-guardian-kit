# Public v1 Release Metadata

`build-summary.json` records the public v1 corpus counts, expected platform files, approximate largest-volume token sizes, and SHA-256 hashes of the generated distribution bundles.

The repository itself contains the unpacked platform knowledge files. GitHub Releases may later provide the matching ZIP bundles for one-click download.
